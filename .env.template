# ─── Industrial AI Knowledge Assistant — Environment Configuration ─────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ─── Application ──────────────────────────────────────────────
ENVIRONMENT=development          # development | staging | production
DEBUG=true                       # Enable debug mode (disable in production)
APP_NAME=Industrial AI Knowledge Assistant
APP_VERSION=1.0.0

# ─── LLM Configuration ───────────────────────────────────────
OPENAI_API_KEY=sk-your-key-here  # Required: OpenAI API key
LLM_MODEL=gpt-3.5-turbo         # gpt-3.5-turbo | gpt-4 | gpt-4-turbo
LLM_TEMPERATURE=0.3             # 0.0 (precise) to 1.0 (creative)
LLM_MAX_TOKENS=1500             # Max tokens per LLM response
LLM_REQUEST_TIMEOUT=30          # Timeout in seconds

# ─── RAG Configuration ───────────────────────────────────────
EMBEDDING_MODEL=text-embedding-ada-002
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
RETRIEVAL_TOP_K=4
# VECTOR_STORE_PATH=/app/data/vectorstore  # Uncomment to persist

# ─── API Configuration ───────────────────────────────────────
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=1
RATE_LIMIT_PER_MINUTE=60
CORS_ORIGINS=["http://localhost:8501","http://localhost:3000"]

# ─── Logging ─────────────────────────────────────────────────
LOG_LEVEL=INFO                   # DEBUG | INFO | WARNING | ERROR
LOG_FORMAT=text                  # json (production) | text (development)
# LOG_FILE=logs/app.log          # Uncomment to enable file logging

# ─── Security ────────────────────────────────────────────────
# API_KEY_HEADER=X-API-Key
# ALLOWED_API_KEYS=["key1","key2"]

# ─── Streamlit UI ────────────────────────────────────────────
UI_PORT=8501
UI_TITLE=Industrial AI Knowledge Assistant
